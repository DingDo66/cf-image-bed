# 最新验证范围（2026-09-22）

当前代码包含回收站、网址上传、Token 和 HEIC；下方 2026-09-13 的云端记录仅适用于旧版本，不能作为新增功能的云端验收证明。

- 本地类型检查、构建和真实 Worker / D1 / R2 集成测试通过。
- 独立 SQLite / 模拟存储测试覆盖迁移和清理失败重试；URL 错误测试使用模拟网络。
- HEIC 浏览器上传、原文件字节保持、JPEG 预览、恢复及永久删除已在本地验证。
- 已检查 390px 手机布局与中英文次级工具入口。
- 生产迁移、Token 上传、网址下载入库、回收站恢复与永久删除已通过实测；测试图片和 Token 已清理。
- 云端 Cron 配置被账户 5 个免费定时任务配额拦截（10072），自动清理尚未启用；没有删除其他项目任务。
- 两个 HEIC 样本在 Images binding 上均返回 9516 解码错误，info 与 transform 路径均已尝试。服务端 HEIC 暂不能验收，浏览器本地转换不受该路径影响。
- 当前本地 33 项测试通过，包含部署配置保留原绑定与自定义定时任务的测试。
- Git 全部 11 个既有提交完成敏感路径及常见密钥模式检查，无匹配；这不是完整安全审计。
- 部署 dry-run 现在与正式部署共用绑定补齐逻辑；不会创建或修改云端资源。

## 历史记录

### 2026-09-13 验证记录

验证日期：2026-09-13。先使用本机 Wrangler 的真实 Worker、D1 和 R2 实例，再部署到实际 Cloudflare 账户进行公网验证；没有使用模拟 API。

## 自动检查

- 前端与 Worker 的 TypeScript 检查通过。
- Vite 生产构建通过。
- `npm run deploy -- --dry-run` 通过：Worker 已成功打包并识别 ASSETS、DB、IMAGES 绑定，没有创建或修改云端资源。
- 12 项 API 集成子测试通过（Node 测试输出包含父测试，共 13 项）：匿名访问隔离、密码登录与 Cookie、跨来源写入拒绝、伪装图片拒绝、真实 PNG 上传与统计、信息编辑与搜索、中文与长文本及标点搜索、相册创建/改名/移动/删除保留图片、公开图片 GET/HEAD/ETag、附件下载文件名、图片删除、退出后的会话吊销。

复现：先运行 `npm run dev`，另一个终端运行 `npm test`。测试仅允许连接本机地址，并清理自身生成的数据。登录限流也在本地生效，不要短时间内连续反复运行测试。

## 浏览器验证

- 桌面 1440 × 1000：四列图片卡片，搜索和上传按钮在同一行，无横向溢出。
- 手机 390 × 844：两列图片、响应式搜索区、可滚动图片详情，无横向溢出。
- 八张本地示例图片均成功加载。
- 登录、中文标签搜索、网格/列表切换、打开图片详情、复制 Markdown 链接已实际操作。
- 通过浏览器文件选择器上传 PNG 到新相册，确认生成 `/t/…webp` 缩略图并成功显示。
- 图片信息保存后与服务器规范化结果同步，保存按钮正确恢复禁用状态。
- 浏览器验证期间未记录到 JavaScript 错误或警告；临时测试图片和相册已清理。

## 部署范围

交付包含交互式部署脚本、可重复执行的数据库迁移、中文维护文档和 GitHub CI。运行 `npm run deploy -- --dry-run` 可进行本地 Worker 打包检查。

已在实际 Cloudflare 账户创建独立 Worker、D1 和 R2，执行迁移并发布，通过 HTTPS 验证以下流程：管理员登录与 Secure Cookie、匿名访问隔离、上传至 R2 并关联 D1 相册、中文搜索与元数据保存、公开原图字节一致性、下载响应、删除后 404、退出后旧会话失效。临时图片与相册均已清理，线上图片库为空。Safari 确认线上登录页正常显示。

部署资源信息、初始管理密码和验证记录保存在被 Git 忽略的 `.deploy/` 中，不包含在共享源码包里。首次使用后如果更换密码，初始密码文件不会自动更新，应自行妥善更新或删除。

尚未验证自定义域名、高并发性能与完整账单周期；这些依赖部署者自己的使用环境。
